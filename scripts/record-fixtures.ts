/**
 * `npm run record:fixtures -- --site <name>` — the ONLY code permitted to hit
 * live APIs (CLAUDE.md rule 2). Captures real OpenTopoData / Overpass responses
 * into fixtures/api/ so every test can run offline and deterministically.
 *
 * Phase 0: placeholder. Implemented as P2.4.
 */
console.log('[record-fixtures] placeholder — implemented in P2.4.');
console.log('[record-fixtures] This is the only script allowed to make live network calls.');
